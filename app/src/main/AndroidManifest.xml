<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="be.magdyabdel.wandz">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MultiplayerConnect"></activity>
        <activity android:name=".ChangeProfileIcon" />
        <activity android:name=".MasterActivity" />
        <activity android:name=".MyWand" />
        <activity android:name=".Multiplayer" />
        <activity
            android:name=".Trainingmode"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ChooseName"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Start"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>